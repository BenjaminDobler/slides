{
  "name": "mcp-server",
  "sourceRoot": "apps/mcp-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx apps/mcp-server/src/index.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
